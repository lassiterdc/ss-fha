---
impact: Medium
urgency: Medium
loe: Low
risk: Low
priority: 2.03
priority-label: Core priority
created: 2026-03-14
description: Address findings from the 2026-03-14 ecosystem conformity audit — architecture.md currency, planning hygiene, codex sentinel gap, and human-decision triage.
---

*Written: 2026-03-14 | Last edit: 2026-03-14 — restructured into multi-phase layout*

# Ecosystem Conformity Remediation

**Generated by**: ecosystem-specialist audit + plan-implementation skill

---

## Task Understanding

### Requirements

Address the 12 actionable findings from `docs/planning/audits/2026-03-14_ecosystem_conformity_audit.md`. The findings fall into two phases:

1. **Phase 1 — Mechanical fixes**: straightforward edits with a clear correct answer (audit actions 1–2, 4–6)
2. **Phase 2 — Human-decision triage doc**: structured checklist for items requiring judgment (audit actions 3, 7–11)

### Assumptions

- The canonical `utility_package_candidates` path is `~/dev/lassiterdc-utils/` (exists on disk), not `~/dev/dcl-utils/` (does not exist). **Risk: Low.**
- Work chunks 03B and 03C are confirmed implemented per git log.
- `audits/` (plural) is the canonical directory name, consistent with `bugs/`, `features/`, `refactors/`.
- Renaming active multi-phase plan directories (audit action 8) is deferred to Phase 2 — blast radius is non-trivial.

### Success Criteria

- Ecosystem-side `architecture.md` accurately reflects current module table and correct `utility_package_candidates` path
- `architecture.md` has `Active Plan References` and `Gotchas` sections
- Work chunks README status accurate for 03B and 03C
- Planning `README.md` documents current naming convention
- `audit/` (singular, stale) consolidated into `audits/` (plural)
- Human-decision items documented as a structured triage checklist

---

## Implementation Strategy

**Two-phase split**: Phase 1 executes all mechanical fixes autonomously. Phase 2 writes a triage checklist for the four human-decision items — not an implementation, a decision artifact.

Phase 1 spans two repos (`ss-fha` and `agentic-workspace`) and requires two commits.

---

## Phase Status

| Phase | Doc | Status |
|-------|-----|--------|
| 1 | `1_mechanical_fixes.md` | Pending |
| 2 | `2_human_decision_triage.md` | Pending |

---

## Risks

- **Cross-repo commits**: Phase 1 edits span `ss-fha` and `agentic-workspace` — two separate commits required.
- **Workspace-local `architecture.md`**: Exists at repo root (`/home/dcl3nd/dev/ss-fha/architecture.md`), not under `src/`. If it does not exist on inspection, skip it — ecosystem copy is authoritative.
- **Dual `architecture.md` divergence**: Nothing in this plan prevents future drift. The Gotchas section calls it out; longer-term fix (symlink or removal) is a Phase 2 decision item.

---

## Definition of Done

- [ ] All phase docs moved to `implemented/`
- [ ] `completed: true` set in this file's YAML frontmatter
- [ ] Entire subdirectory moved to `docs/planning/audits/completed/`
- [ ] `scripts/generate_planning_tables.py --planning-dir docs/planning` run (if script exists)
